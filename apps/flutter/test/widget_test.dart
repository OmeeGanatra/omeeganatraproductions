import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('OGP app smoke test', (WidgetTester tester) async {
    // Placeholder smoke test — full widget tests require Firebase mocking.
    expect(true, isTrue);
  });
}
